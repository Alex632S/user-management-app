name: Main CI/CD Pipeline

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  setup:
    name: Setup & Cache
    runs-on: ubuntu-latest
    outputs:
      cache-key: ${{ steps.final-key.outputs.key }}
      node-version: ${{ steps.node-version.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        id: setup-node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Save Node version
        id: node-version
        run: echo "version=$(node --version)" >> $GITHUB_OUTPUT

      - name: Calculate cache key
        id: calc-key
        run: |
          KEY="${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}-$(node --version)"
          echo "key=$KEY" >> $GITHUB_OUTPUT

      - name: Cache node_modules
        id: cache
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            ~/.npm
          key: ${{ steps.calc-key.outputs.key }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies (if no cache)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci --prefer-offline --no-audit --no-fund
        env:
          NODE_ENV: development

      - name: Set final cache key
        id: final-key
        run: echo "key=${{ steps.calc-key.outputs.key }}" >> $GITHUB_OUTPUT

      - name: Generate Nuxt types
        run: npx nuxt prepare
        continue-on-error: true

  lint:
    name: Lint & Typecheck
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Calculate cache key
        id: calc-key
        run: |
          KEY="${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}-$(node --version)"
          echo "key=$KEY" >> $GITHUB_OUTPUT

      - name: Restore cache
        uses: actions/cache@v4
        id: restore-cache
        with:
          path: node_modules
          key: ${{ steps.calc-key.outputs.key }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install if no cache
        if: steps.restore-cache.outputs.cache-hit != 'true'
        run: npm ci --prefer-offline --no-audit

      - name: Generate Nuxt types
        run: npx nuxt prepare
        continue-on-error: true

      - name: TypeScript check
        run: npm run typecheck
        continue-on-error: true

      - name: ESLint
        run: npm run lint
        continue-on-error: true

  test:
    name: Tests
    needs: [setup, lint]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        shard: [1, 2, 3]
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Calculate cache key
        run: echo "key=${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}" >> $GITHUB_ENV

      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ env.key }}

      - name: Install dependencies if needed
        if: steps.restore-cache.outputs.cache-hit != 'true'
        run: npm ci --prefer-offline

      - name: Generate Nuxt types
        run: npx nuxt prepare

      - name: Run tests
        run: npm run test -- --shard=${{ matrix.shard }}/${{ strategy.job-total }}
        continue-on-error: true

  test-report:
    name: Test Report
    needs: test
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check test status
        run: |
          if [[ "${{ needs.test.result }}" == "failure" ]]; then
            exit 1
          fi

  build:
    name: Build
    needs: [setup, test-report]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Calculate cache key
        run: echo "key=${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}" >> $GITHUB_ENV

      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ env.key }}

      - name: Install dependencies if needed
        if: steps.restore-cache.outputs.cache-hit != 'true'
        run: npm ci --prefer-offline

      - name: Clean previous build
        run: |
          rm -rf .nuxt .output dist .netlify/functions-internal

      - name: Build project for Netlify
        run: npm run build
        env:
          NITRO_PRESET: netlify
          NODE_ENV: production

      - name: Verify build output
        run: |
          echo "🔍 Проверка структуры сборки:"

          if [ -d "dist" ]; then
            echo "✅ Папка dist найдена"
            echo "   Содержит $(find dist -type f | wc -l) файлов"
          else
            echo "❌ Папка dist не найдена!"
            exit 1
          fi

          if [ -d ".netlify/functions-internal" ]; then
            echo "✅ Папка функций найдена"
            echo "   Содержит $(find .netlify/functions-internal -type f | wc -l) файлов"
          else
            echo "⚠️ Папка функций не найдена (может быть норм для статики)"
          fi

          echo "📁 Содержимое dist:"
          ls -la dist/

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ github.sha }}
          path: |
            dist
            .netlify/functions-internal
            netlify.toml
          retention-days: 30

  deploy:
    name: Deploy
    needs: [build, test-report]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment:
      name: production
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-${{ github.sha }}
          path: .

      - name: Verify downloaded artifacts
        run: |
          echo "🔍 Проверка загруженных артефактов:"
          ls -la

          if [ -d "dist" ]; then
            echo "✅ dist/ существует"
          else
            echo "❌ dist/ не найден!"
            exit 1
          fi

      - name: Install Netlify CLI
        run: npm install -g netlify-cli --no-fund

      - name: Deploy to Netlify
        id: deploy
        run: |
          # Определяем параметры деплоя
          DEPLOY_ARGS="--dir=dist --prod --message=Deploy ${{ github.sha }}"

          # Добавляем функции если есть
          if [ -d ".netlify/functions-internal" ]; then
            DEPLOY_ARGS="$DEPLOY_ARGS --functions=.netlify/functions-internal"
            echo "⚡ Функции будут загружены из .netlify/functions-internal"
          fi

          echo "🚀 Запускаем деплой: netlify deploy $DEPLOY_ARGS"

          # Выполняем деплой
          OUTPUT=$(npx netlify-cli deploy $DEPLOY_ARGS 2>&1)
          EXIT_CODE=$?

          # Сохраняем вывод для отладки
          echo "$OUTPUT" > deploy-output.log

          # Парсим URL из вывода
          URL=$(echo "$OUTPUT" | grep -oP 'Production deploy is live.*?https://[^\s]+' | grep -oP 'https://[^\s]+' | head -1)

          if [ -z "$URL" ]; then
            URL=$(echo "$OUTPUT" | grep -oP 'Website URL: +\Khttps://[^\s]+')
          fi

          if [ -z "$URL" ]; then
            # Пробуем найти любой URL в выводе
            URL=$(echo "$OUTPUT" | grep -oP 'https://[^\s]+' | head -1)
          fi

          # Сохраняем URL в выходные параметры
          echo "url=$URL" >> $GITHUB_OUTPUT

          # Проверяем успешность деплоя
          if echo "$OUTPUT" | grep -q "Deploy is live\|Production deploy is live\|Website URL:"; then
            echo "✅ Деплой успешно завершен!"
            echo "🌍 URL: $URL"
            
            # Показываем статистику деплоя
            echo "📊 Статистика деплоя:"
            echo "$OUTPUT" | grep -E "files uploaded|functions uploaded|size" || true
            
            exit 0
          else
            echo "❌ Ошибка деплоя!"
            echo "📄 Последние 20 строк вывода:"
            echo "$OUTPUT" | tail -20
            exit 1
          fi
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      - name: Health check
        if: success()
        run: |
          URL="${{ steps.deploy.outputs.url }}"

          if [ -z "$URL" ] || [ "$URL" = "null" ]; then
            echo "⚠️ URL не получен, пропускаем health check"
            exit 0
          fi

          echo "🔍 Проверяем доступность: $URL"

          for i in {1..10}; do
            echo "⏳ Попытка $i/10..."
            
            # Проверяем HTTP статус
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$URL" 2>/dev/null || echo "000")
            
            if [ "$HTTP_STATUS" = "200" ] || [ "$HTTP_STATUS" = "301" ] || [ "$HTTP_STATUS" = "302" ]; then
              echo "✅ Сайт доступен! (HTTP $HTTP_STATUS)"
              exit 0
            fi
            
            echo "⏳ Статус: $HTTP_STATUS, ждем 3 секунды..."
            sleep 3
          done

          echo "❌ Сайт не отвечает после 10 попыток"
          exit 1

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: v${{ github.run_number }}
          name: Release v${{ github.run_number }}
          generate_release_notes: true
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify success
        if: success()
        run: |
          echo "🎉 Деплой успешно завершен!"
          echo "🌍 Сайт доступен по адресу: ${{ steps.deploy.outputs.url }}"
          echo "📦 Версия: v${{ github.run_number }}"
          echo "🔗 Коммит: ${{ github.sha }}"
